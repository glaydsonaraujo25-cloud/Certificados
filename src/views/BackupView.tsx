import React,{useRef,useState}from'react';
import{Download,Upload,ShieldCheck,CheckCircle2,AlertTriangle}from'lucide-react';
const APP_KEYS=['certifyai_institution','certifyai_courses','certifyai_students','certifyai_certificates','certifyai_theme','certifyai_data_version'];
export const BackupView:React.FC=()=>{const inputRef=useRef<HTMLInputElement>(null);const[message,setMessage]=useState<{type:'success'|'error';text:string}|null>(null);
const exportBackup=()=>{const data:Record<string,unknown>={};APP_KEYS.forEach(key=>{const raw=localStorage.getItem(key);if(raw===null)return;try{data[key]=JSON.parse(raw)}catch{data[key]=raw}});const payload={format:'certificados-cvte-backup',version:1,exportedAt:new Date().toISOString(),data};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'});const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=`backup-cvte-${new Date().toISOString().slice(0,10)}.json`;document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url);setMessage({type:'success',text:'Backup local baixado com sucesso.'});};
const importBackup=async(file:File)=>{try{const parsed=JSON.parse(await file.text());if(parsed?.format!=='certificados-cvte-backup'||!parsed?.data||typeof parsed.data!=='object')throw new Error('Arquivo de backup CVTE incompatível.');if(!window.confirm('Restaurar este backup? Os dados locais atuais serão substituídos.'))return;APP_KEYS.forEach(key=>localStorage.removeItem(key));for(const[key,value]of Object.entries(parsed.data)){if(APP_KEYS.includes(key))localStorage.setItem(key,typeof value==='string'?value:JSON.stringify(value));}setMessage({type:'success',text:'Backup restaurado. Recarregando o sistema...'});setTimeout(()=>window.location.reload(),800);}catch(error){setMessage({type:'error',text:error instanceof Error?error.message:'Não foi possível restaurar o backup.'});}};
return <div className="p-4 sm:p-6 lg:p-8 space-y-6 max-w-4xl mx-auto w-full"><div><div className="flex items-center gap-2 text-indigo-600 text-xs font-bold uppercase tracking-wider mb-1"><ShieldCheck className="w-4 h-4"/>Proteção local</div><h1 className="text-2xl sm:text-3xl font-extrabold">Backup dos dados</h1><p className="text-sm text-slate-500 mt-1">Salve uma cópia dos condutores, certificados e configurações deste navegador.</p></div>{message&&<div className={`rounded-xl border px-4 py-3 text-sm flex items-center gap-2 ${message.type==='success'?'bg-emerald-50 border-emerald-200 text-emerald-800':'bg-rose-50 border-rose-200 text-rose-800'}`}>{message.type==='success'?<CheckCircle2 className="w-4 h-4"/>:<AlertTriangle className="w-4 h-4"/>}{message.text}</div>}<div className="grid md:grid-cols-2 gap-5"><Card icon={<Download className="w-5 h-5"/>} title="Baixar backup" text="Gera um arquivo JSON com os dados usados pelo sistema CVTE."><button onClick={exportBackup} className="w-full py-2.5 rounded-xl bg-indigo-600 text-white text-sm font-bold flex items-center justify-center gap-2"><Download className="w-4 h-4"/>Baixar backup</button></Card><Card icon={<Upload className="w-5 h-5"/>} title="Restaurar backup" text="Recupere os dados a partir de um backup gerado por esta versão."><input ref={inputRef} type="file" accept="application/json,.json" className="hidden" onChange={e=>{const file=e.target.files?.[0];if(file)importBackup(file);e.currentTarget.value='';}}/><button onClick={()=>inputRef.current?.click()} className="w-full py-2.5 rounded-xl bg-emerald-600 text-white text-sm font-bold flex items-center justify-center gap-2"><Upload className="w-4 h-4"/>Selecionar backup</button></Card></div><div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-xs text-amber-900"><strong>Importante:</strong> os dados ficam somente neste navegador. Faça um backup antes de limpar dados do navegador, trocar de computador ou fazer alterações importantes.</div></div>};
const Card:React.FC<{icon:React.ReactNode;title:string;text:string;children:React.ReactNode}>=({icon,title,text,children})=><div className="bg-white dark:bg-slate-900 rounded-2xl border p-6 space-y-4"><div className="w-11 h-11 rounded-xl bg-indigo-50 text-indigo-600 flex items-center justify-center">{icon}</div><div><h2 className="font-extrabold text-lg">{title}</h2><p className="text-xs text-slate-500 mt-1">{text}</p></div>{children}</div>;
