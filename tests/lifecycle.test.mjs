import test from 'node:test';
import assert from 'node:assert/strict';
import {expiryDate,certificateState,validateClass} from '../src/utils/lifecycle.ts';
import {validateBackup} from '../src/utils/persistence.ts';
test('validade trata ano bissexto e data inválida',()=>{assert.equal(expiryDate('2024-02-29',5),'2029-02-28');assert.throws(()=>expiryDate('2026-02-30',5));assert.throws(()=>expiryDate('2026-01-01',0));});
test('vencimento inclusivo, 60 dias e cancelamento',()=>{assert.equal(certificateState({status:'active',expiresAt:'2026-09-10'},'2026-09-10'),'soon');assert.equal(certificateState({status:'active',expiresAt:'2026-09-09'},'2026-09-10'),'expired');assert.equal(certificateState({status:'cancelled',expiresAt:'2020-01-01'},'2026-09-10'),'cancelled');assert.equal(certificateState({status:'active'},'2026-09-10'),'unknown');assert.equal(certificateState({status:'active',expiresAt:'2026-11-09'},'2026-09-10'),'soon');assert.equal(certificateState({status:'active',expiresAt:'2026-11-10'},'2026-09-10'),'active');});
test('backup aceita versão antiga e preserva turmas novas',()=>{const backup={format:'certificados-cvte-backup',version:1,data:{certifyai_institution:{name:'I'},certifyai_students:[],certifyai_courses:[],certifyai_certificates:[]}};assert.ok(validateBackup(backup));const turma={id:'t1',name:'T',courseId:'c1',startDate:'2026-01-01',endDate:'2026-01-02',createdAt:'2026-01-01',instructorName:'I',studentIds:[],validityYears:5};backup.data.certifyai_classes=[turma];assert.equal(validateBackup(backup).certifyai_classes[0].id,'t1');backup.data.certifyai_classes[0].studentIds='invalid';assert.throws(()=>validateBackup(backup));});
