import React, { useState } from 'react';
import { useApp } from '../context/AppContext';
import { CertificateFrontPage } from '../components/CertificateDocument';
import { Award, Building2, CheckCircle, Moon, RotateCcw, Save, Shield, Sun, Trash2, UserCog } from 'lucide-react';

export const SettingsView: React.FC = () => {
  const { institution, updateInstitution, resetToDemoData, clearAllData, theme, toggleTheme, user, switchUserRole } = useApp();
  const [activeTab, setActiveTab] = useState<'model' | 'institution' | 'access' | 'appearance' | 'data'>('model');
  const [name, setName] = useState(institution.name);
  const [institutionCnpj, setInstitutionCnpj] = useState(institution.institutionCnpj || '');
  const [email, setEmail] = useState(institution.email || '');
  const [phone, setPhone] = useState(institution.phone || '');
  const [website, setWebsite] = useState(institution.website || '');
  const [city, setCity] = useState(institution.city || 'Brasília');
  const [state, setState] = useState(institution.state || 'DF');
  const [signatoryName, setSignatoryName] = useState(institution.signatoryName || '');
  const [signatoryRole, setSignatoryRole] = useState(institution.signatoryRole || 'Diretor Geral');
  const [signatoryCpf, setSignatoryCpf] = useState(institution.signatoryCpf || '');
  const [signatureImageUrl, setSignatureImageUrl] = useState(institution.signatureImageUrl || '');
  const [sealText, setSealText] = useState(institution.sealText || 'DOCUMENTO AUTÊNTICO • FORTE CAXIAS');
  const [defaultCertificateText, setDefaultCertificateText] = useState(institution.defaultCertificateText || 'A Base Administrativa do Quartel-General do Exército – Forte Caxias certifica que [NOME DO ALUNO], inscrito no CPF nº [CPF], concluiu com aproveitamento o curso [NOME DO CURSO], realizado no período de [DATA INICIAL] a [DATA FINAL], com carga horária total de [CARGA HORÁRIA] horas, desenvolvendo competências em informática, produtividade digital e uso responsável de ferramentas de Inteligência Artificial.');
  const [saved, setSaved] = useState(false);
  const isAdmin = user?.role === 'admin';

  const save = (event: React.FormEvent) => {
    event.preventDefault();
    if (!isAdmin) { alert('Apenas o perfil Administrador pode alterar o modelo oficial.'); return; }
    updateInstitution({ name, institutionCnpj, email, phone, website, city, state, signatoryName, signatoryRole, signatoryCpf, signatureImageUrl, sealText, defaultCertificateText, legalInstruction: '', contranResolution: '', validityText: 'certificação referente à conclusão e ao aproveitamento no curso', codeFormat: 'sequential' });
    setSaved(true); window.setTimeout(() => setSaved(false), 2500);
  };

  const preview = {
    studentName: 'NOME DO ALUNO', studentDocument: '000.000.000-00', courseName: 'Operador de Computador com IA', courseSubhead: 'Operador de Computador com Inteligência Artificial', workloadHours: 230, issueDate: new Date().toISOString().split('T')[0], startDate: '2026-06-01', endDate: '2026-08-26', location: `${city}-${state}`, institutionName: name, institutionCnpj, signatoryName, signatoryRole, signatoryCpf, signatureImageUrl, code: 'CERT-2026-000001'
  };

  const tabs = [
    ['model', 'Modelo Oficial', Award], ['institution', 'Instituição', Building2], ['access', 'Acesso', UserCog], ['appearance', 'Aparência', theme === 'dark' ? Sun : Moon], ['data', 'Dados', Shield]
  ] as const;

  return <div className="p-4 sm:p-6 lg:p-8 space-y-6 max-w-6xl mx-auto w-full">
    <div className="flex items-center justify-between gap-4"><div><h1 className="text-2xl sm:text-3xl font-extrabold text-slate-900 dark:text-white">Configurações & Modelo Oficial</h1><p className="text-sm text-slate-500 mt-1">Configuração do certificado do curso Operador de Computador com IA.</p></div>{saved && <div className="flex items-center gap-2 px-3 py-2 rounded-xl bg-emerald-100 text-emerald-700 text-xs font-bold"><CheckCircle className="w-4 h-4" />Salvo</div>}</div>
    <div className="flex flex-wrap gap-2 border-b border-slate-200 dark:border-slate-800 pb-2">{tabs.map(([id,label,Icon]) => <button key={id} onClick={() => setActiveTab(id)} className={`flex items-center gap-2 px-4 py-2 rounded-xl text-xs font-bold ${activeTab === id ? 'bg-indigo-600 text-white' : 'bg-white dark:bg-slate-900 border border-slate-200 dark:border-slate-800'}`}><Icon className="w-4 h-4" />{label}</button>)}</div>

    <form onSubmit={save} className="space-y-5">
      {activeTab === 'model' && <div className="space-y-5">
        <section className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 p-6 space-y-4"><div><h2 className="font-bold">Texto oficial do certificado</h2><p className="text-xs text-slate-500">Use as variáveis para personalizar o texto sem alterar o layout.</p></div><textarea rows={6} value={defaultCertificateText} onChange={(e) => setDefaultCertificateText(e.target.value)} className="w-full px-3 py-3 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 text-sm" /><div className="flex flex-wrap gap-2 text-[11px]">{['[NOME DO ALUNO]','[CPF]','[NOME DO CURSO]','[CARGA HORÁRIA]','[DATA INICIAL]','[DATA FINAL]'].map((tag) => <button type="button" key={tag} onClick={() => setDefaultCertificateText((text) => `${text} ${tag}`)} className="px-2 py-1 rounded-lg bg-slate-100 dark:bg-slate-800 font-mono">{tag}</button>)}</div><label className="block text-xs font-semibold">Texto do selo<input value={sealText} onChange={(e) => setSealText(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label></section>
        <section className="bg-slate-200 dark:bg-slate-950 rounded-2xl p-4 overflow-x-auto"><div className="origin-top scale-[0.72] sm:scale-[0.85] lg:scale-95"><CertificateFrontPage certificate={preview} elementId="settings-certificate-preview" /></div></section>
      </div>}

      {activeTab === 'institution' && <section className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 p-6 space-y-4"><h2 className="font-bold">Dados da Instituição</h2><div className="grid sm:grid-cols-2 gap-3"><label className="text-xs font-semibold sm:col-span-2">Nome<input value={name} onChange={(e) => setName(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">CNPJ<input value={institutionCnpj} onChange={(e) => setInstitutionCnpj(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">E-mail<input value={email} onChange={(e) => setEmail(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">Telefone<input value={phone} onChange={(e) => setPhone(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">Site<input value={website} onChange={(e) => setWebsite(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">Cidade<input value={city} onChange={(e) => setCity(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">UF<input value={state} onChange={(e) => setState(e.target.value.toUpperCase())} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label></div><div className="grid sm:grid-cols-2 gap-3 pt-3 border-t border-slate-100 dark:border-slate-800"><label className="text-xs font-semibold">Signatário<input value={signatoryName} onChange={(e) => setSignatoryName(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">Função<input value={signatoryRole} onChange={(e) => setSignatoryRole(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">CPF do signatário<input value={signatoryCpf} onChange={(e) => setSignatoryCpf(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label><label className="text-xs font-semibold">URL/Data da assinatura<input value={signatureImageUrl} onChange={(e) => setSignatureImageUrl(e.target.value)} className="mt-1 w-full px-3 py-2 rounded-xl bg-slate-50 dark:bg-slate-800 border border-slate-200 dark:border-slate-700" /></label></div></section>}

      {activeTab === 'access' && <section className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 p-6"><h2 className="font-bold">Controle de Acesso</h2><p className="text-sm text-slate-500 mt-1 mb-4">Perfil atual: <strong>{user?.role === 'admin' ? 'Administrador' : 'Operador'}</strong></p><button type="button" onClick={() => switchUserRole(user?.role === 'admin' ? 'operator' : 'admin')} className="px-4 py-2 rounded-xl bg-indigo-600 text-white text-sm font-bold">Alternar para {user?.role === 'admin' ? 'Operador' : 'Administrador'}</button></section>}
      {activeTab === 'appearance' && <section className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 p-6"><h2 className="font-bold">Aparência</h2><p className="text-sm text-slate-500 mt-1 mb-4">O tema claro permanece como padrão principal.</p><button type="button" onClick={toggleTheme} className="flex items-center gap-2 px-4 py-2 rounded-xl bg-slate-100 dark:bg-slate-800 text-sm font-bold">{theme === 'dark' ? <Sun className="w-4 h-4" /> : <Moon className="w-4 h-4" />}Usar tema {theme === 'dark' ? 'claro' : 'escuro'}</button></section>}
      {activeTab === 'data' && <section className="grid md:grid-cols-2 gap-4"><div className="bg-white dark:bg-slate-900 rounded-2xl border border-slate-200 dark:border-slate-800 p-6"><h2 className="font-bold">Restaurar dados padrão</h2><p className="text-sm text-slate-500 mt-1 mb-4">Restaura o curso Operador de Computador com IA, aluno de exemplo e certificado de demonstração.</p><button type="button" onClick={() => { if (confirm('Restaurar dados padrão do curso Operador de Computador com IA?')) { resetToDemoData(); window.location.reload(); } }} className="flex items-center gap-2 px-4 py-2 rounded-xl bg-amber-100 text-amber-800 text-sm font-bold"><RotateCcw className="w-4 h-4" />Restaurar</button></div><div className="bg-white dark:bg-slate-900 rounded-2xl border border-rose-200 dark:border-rose-900 p-6"><h2 className="font-bold text-rose-700">Limpar dados locais</h2><p className="text-sm text-slate-500 mt-1 mb-4">Remove cursos, alunos, certificados e auditoria salvos neste navegador.</p><button type="button" onClick={() => { if (confirm('Apagar todos os dados locais?')) { clearAllData(); window.location.reload(); } }} className="flex items-center gap-2 px-4 py-2 rounded-xl bg-rose-600 text-white text-sm font-bold"><Trash2 className="w-4 h-4" />Limpar dados</button></div></section>}

      {(activeTab === 'model' || activeTab === 'institution') && <div className="flex justify-end"><button type="submit" disabled={!isAdmin} className="flex items-center gap-2 px-5 py-2.5 rounded-xl bg-indigo-600 hover:bg-indigo-700 text-white text-sm font-bold disabled:opacity-50"><Save className="w-4 h-4" />Salvar Configurações</button></div>}
    </form>
  </div>;
};
